# Source for EECS-E6895 Project

## Building
This project uses Maven. To build a (clean) install, use the following command
from the top directory (i.e., where ```pom.xml``` is located):
```
mvn clean install
```